# Environment Assets

Place simulator environment files here.

Expected layout:

```text
envs/
└── airsim/
    ├── env_7/
    ├── env_8/
    └── ...
```

These environment files are distributed separately (see the project website Usage page).
